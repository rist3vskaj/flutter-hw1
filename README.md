## Кототиндер — Домашняя работа №1

**Кототиндер** — это небольшое Flutter‑приложение, где можно свайпать случайных котиков, ставить им лайки и изучать список пород из API `thecatapi.com`.

---

### Актуальное описание проекта

- **Главный экран** показывает случайного котика с названием его породы.
- **Свайп или кнопки лайк/дизлайк** переключают котика на нового.
- **Счётчик лайков** увеличивается при свайпе вправо или нажатии на кнопку лайка.
- **По нажатию на изображение** открывается экран с детальным описанием породы.
- **Таб‑бар** позволяет переключаться между вкладками «Котики» и «Список пород».
- **Экран списка пород** показывает все доступные породы и краткую информацию о них; по тапу открывается детальный экран.
- Используется `http` для запросов к `https://thecatapi.com`, картинки загружаются через `CachedNetworkImage`.
- Обработаны сетевые ошибки: при проблемах показывается диалог с описанием ошибки.
- Подключены `flutter_lints`, код отформатирован через `dart format`.

---

### Реализованные фичи (чек‑лист под критерии)

- **Главный экран**
  - [x] Отображается случайное изображение котика и название породы (endpoint `/images/search` у TheCatAPI).
  - [x] Есть свайп влево/вправо (`Dismissible`) и две кнопки: лайк/дизлайк.
  - [x] Любое действие (свайп или кнопки) подгружает нового котика.
  - [x] Счётчик лайков увеличивается при лайке и свайпе вправо.
  - [x] При нажатии на изображение открывается экран детального описания.

- **Экран детального описания котика/породы**
  - [x] Отображается то же изображение котика.
  - [x] Отображается подробная информация о породе: описание, происхождение, характер, продолжительность жизни, интеллект, энергичность, ласковость.

- **Таб‑бар и список пород**
  - [x] Реализован `TabBar` с двумя вкладками: «Котики» и «Список пород».
  - [x] На экране «Список пород» отображается список всех пород с краткой информацией.
  - [x] По тапу на породу открывается детальный экран с описанием и 3–4 характеристиками.

- **Технические требования**
  - [x] Использован пакет `http` для запросов к API `https://thecatapi.com`.
  - [x] Случайная картинка котика получается через endpoint `/images/search`.
  - [x] Породы загружаются через endpoint `/breeds`.
  - [x] Для отображения картинок используется `CachedNetworkImage`.
  - [x] Подключён `flutter_lints` (см. `analysis_options.yaml`), проект проходит `flutter analyze` при корректной настройке SDK.
  - [x] Есть обработка сетевых ошибок с показом диалога `AlertDialog`.

- **Визуал и UX**
  - [x] Современный визуал на основе Material 3.
  - [x] Акцентный цвет — розовый (`Colors.pinkAccent`), подчёркивающий «тиндер‑стиль».
  - [x] Используются карточки с закруглёнными углами, тени, чип со счётчиком лайков.

---

### Скриншоты интерфейса

Замените плейсхолдеры на ваши реальные скриншоты из эмулятора/устройства.

- **Главный экран с котиком**
  - `screenshots/home_cat.png`
- **Экран детального описания котика**
  - `screenshots/cat_detail.png`
- **Список пород**
  - `screenshots/breed_list.png`
- **Детальный экран породы**
  - `screenshots/breed_detail.png`

> Поместите PNG/JPEG‑файлы в папку `screenshots/` в корне проекта и обновите пути/названия в этом разделе по необходимости.

---

### APK — ссылка на скачивание

1. Соберите релизную версию:

   ```bash
   flutter build apk --release
   ```

2. Получившийся файл будет находиться по пути:

   - `build/app/outputs/flutter-apk/app-release.apk`

3. Загрузите его в любое удобное место (Google Drive, GitHub Releases, облако и т.п.) и вставьте ссылку ниже:

- **Актуальная ссылка на APK:**  
  `https://example.com/kototinder-latest.apk` ← **замените на вашу реальную ссылку**

---

### Как запустить проект локально

1. Установите Flutter SDK (стабильный канал, версия 3.x).
2. В корне проекта выполните:

   ```bash
   flutter pub get
   flutter run
   ```

3. При необходимости укажите `THECATAPI_API_KEY` в переменной окружения или в настройках эмулятора (TheCatAPI позволяет использовать и публичный доступ без ключа, но лимиты могут быть ниже).

---

### Структура проекта (основные файлы)

- `lib/main.dart` — точка входа, навигация, таб‑бар, главный экран, экран списка пород и детальные экраны.
- `lib/models.dart` — модели `CatBreed` и `CatImage` для работы с данными из TheCatAPI.
- `pubspec.yaml` — зависимости (`http`, `cached_network_image`, `flutter_lints`, `flutter_launcher_icons`) и настройки иконки.
- `analysis_options.yaml` — включён `flutter_lints` и дополнительные правила.

---

### Полезные команды

- Форматирование кода:

  ```bash
  dart format lib
  ```

- Статический анализ:

  ```bash
  flutter analyze
  ```

---

### Дополнительные заметки

- Для кастомной иконки приложения в `pubspec.yaml` уже настроен пакет `flutter_launcher_icons`.  
  Положите ваше изображение иконки в `assets/images/app_icon.png` и выполните:

  ```bash
  flutter pub run flutter_launcher_icons
  ```

- В случае сетевых ошибок (нет интернета, проблемы с API) пользователь увидит диалог с текстом ошибки и может попробовать ещё раз.


